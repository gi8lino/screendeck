apiVersion: v1
kind: Service
metadata:
  name: screendeck
spec:
  selector:
    app.kubernetes.io/name: screendeck
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  type: ClusterIP
