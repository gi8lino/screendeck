name: screendeck

services:
  screendeck:
    build: .
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      SCREENDECK__BASE_URL: ${SCREENDECK_BASE_URL:-http://localhost:8080}
    volumes:
      - screendeck-data:/data

volumes:
  screendeck-data:
